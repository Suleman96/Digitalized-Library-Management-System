# ── API Framework ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-multipart>=0.0.12

# ── Data & Search ────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
faiss-cpu>=1.7.4          # use faiss-gpu if CUDA is available
sentence-transformers>=2.7.0
torch>=2.2.0              # CPU build is fine; GPU optional

# ── HTTP / External APIs ─────────────────────────────────────
requests>=2.31.0

# ── Environment & Config ─────────────────────────────────────
python-dotenv>=1.0.0

# ── PDF export ───────────────────────────────────────────────
fpdf2>=2.7.9

# ── RAG / LangChain stack ────────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langgraph>=0.2.0
networkx>=3.2              # knowledge graph for hybrid retrieval

# ── AI Providers (install the ones you need) ─────────────────
# Claude (Anthropic) — recommended for this session
langchain-anthropic>=0.3.0
anthropic>=0.40.0

# OpenAI — optional
# langchain-openai>=0.2.0
# openai>=1.50.0

# Google Gemini — optional
# langchain-google-genai>=2.0.0
# google-generativeai>=0.8.0

# Ollama (local, free) — optional
# langchain-ollama>=0.2.0
# ollama>=0.3.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.3.0
pytest-cov>=5.0.0
httpx>=0.27.0            # FastAPI TestClient transport
pytest-asyncio>=0.24.0

# ── Dev / Linting (optional, not needed on server) ───────────
# ruff
# black
# mypy
